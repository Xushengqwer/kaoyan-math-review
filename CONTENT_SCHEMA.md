# 内容数据格式规范

每个学科一个 JS 文件，放在 `assets/data/`，用 `<script>` 标签直接引入（不走 fetch，避免本地 file:// 打开时的跨域问题）。文件内容格式如下（整个文件就是一次函数调用，不需要 export/import，不需要 IIFE 包裹）：

```js
registerSubject({
  id: "calculus",              // 学科唯一 id，英文小写短横线
  name: "高等数学（微积分）",     // 学科中文名，显示用
  color: "#2563eb",             // 学科主题色（十六进制），用于 UI 强调色
  chapters: [
    { id: "limit", name: "函数、极限、连续", order: 1 },
    { id: "derivative", name: "一元函数微分学", order: 2 }
    // ...每章一个对象，order 从 1 开始递增，决定展示顺序
  ],
  items: [
    {
      id: "calc-limit-def-eps-n",   // 全局唯一 id，建议 学科缩写-章节缩写-def/thm/prop-序号/关键词
      chapterId: "limit",           // 必须对应上面 chapters 里的某个 id
      type: "definition",           // 只能是 "definition" | "theorem" | "property" 三选一
      title: "数列极限的 ε-N 定义",   // 简短标题（10~20字内），用于卡片正面 / 列表展示
      statement: "设数列 $\\{x_n\\}$，若存在常数 $A$，对任意给定的 $\\varepsilon>0$，总存在正整数 $N$，使得当 $n>N$ 时恒有 $|x_n-A|<\\varepsilon$，则称数列 $\\{x_n\\}$ 收敛于 $A$，记作 $\\lim\\limits_{n\\to\\infty}x_n=A$。",
      // statement: 严谨完整的定义/定理/性质表述本身，必须是这个考点最核心、最需要背诵的一段话。
      // 其中的数学公式一律用 KaTeX 语法，行内公式用单个 $...$ 包裹，不要用 \( \)，不要用 $$...$$（多行公式可用 \begin{aligned}...\end{aligned} 包在单个 $...$ 或需要独占一行时用两个连续的 $$...$$ 也可以，但同一文件内保持风格统一，优先单 $）。
      // JS 字符串里所有反斜杠必须写成 \\，例如 \\lim \\varepsilon \\frac{a}{b}。
      explanation: "几何意义：数列从某一项之后，所有的点都落在区间 $(A-\\varepsilon, A+\\varepsilon)$ 内。记忆口诀：\"任意小的 ε，总能找到 N，N 之后都在范围内\"。常见易错点：N 依赖于 ε，ε 越小 N 通常越大。",
      // explanation: 补充记忆技巧 / 直观理解 / 易混淆点 / 常见错误提醒，2~4 句话，帮助"背和记忆"，不是重复 statement。
      tags: ["极限", "数列", "ε-N语言"],
      // tags: 2~4 个关键词，用于搜索

      diagram: `<svg viewBox="0 0 320 180" xmlns="http://www.w3.org/2000/svg">...</svg>`,
      // diagram: 可选。一段自包含的内联 SVG 字符串（用反引号包裹，避免和内部双引号冲突）。
      // 只在真正有直观几何图像、能帮助记忆的条目上加（导数的几何意义、中值定理、正态分布曲线、特征向量方向不变等），
      // 不要每条都加，绝大多数条目不需要图。
      // 颜色规范：结构线（坐标轴、辅助线）用 stroke="currentColor"（自动适配深色模式）；
      // 需要强调的曲线/向量用 stroke="#3b82f6"（蓝）作主强调色，次强调可用 stroke="#b5490f"（橙）。
      // 不要引入外部图片或字体，纯 <svg> 矢量绘图，viewBox 建议 300~320 宽、100~200 高。
      diagramCaption: "一句话说明这张图在讲什么"
      // diagramCaption: 可选，配合 diagram 使用，图下方的小字说明。没有 diagram 就不要写这个字段。
    }
    // ... 每章建议 10~18 个条目，覆盖该章最核心、最高频考查的定义/定理/性质
  ]
});
```

## 排版格式：statement / explanation 里可以用的 HTML

`statement` 和 `explanation` 是直接当 HTML 插入页面的（不是纯文本），为了让内容读起来清爽、好记，鼓励按下面的规则加排版标记：

- **`<strong>关键词</strong>`**：加粗真正的关键词——被定义的术语本身、核心结论、"充要条件"这类分水岭词、易混淆的限定词（如"非零""互不相同""至少一个"）。不要整段话都加粗，加粗是为了让人一眼扫到重点，滥用等于没用。
- **`<u>一句话</u>`**：给整条里最核心的那一句结论/公式描述加下划线，全条最多用一次，可加可不加，不是必须的。
- **`<ul><li>…</li></ul>`**：当一条 statement 或 explanation 里天然存在 2 个以上并列的情形/条件/分类时（例如"水平渐近线…；铅直渐近线…；斜渐近线…"，或"第一类错误…第二类错误…"，或几种判别法并列），拆成 `<li>`，每条前面可以用 `<strong>` 标出这一类的名字（如 `<li><strong>水平渐近线：</strong>...</li>`）。只有单一陈述、没有并列结构的条目**不要**硬拆成列表。
- 公式 `$...$` 内部不要插入任何 HTML 标签，HTML 标签只包裹公式外的中文/结论文字；也不要在标签里加 class/style 等属性，用最朴素的 `<strong>` `<u>` `<ul>` `<li>` 就够。
- 这些标签会让字符串变长，注意 JS 字符串里的双引号仍然只能用 `\"` 转义，标签本身不含引号所以不会有额外转义负担。

## 硬性要求

1. **数学内容必须准确无误**，这是考研复习资料，错误定义/定理会直接误导考生。拿不准的表述，使用教材通行的标准说法（同济高等数学、线性代数教材，浙大概率论与数理统计教材的口径）。
2. `type` 字段严格三选一：`definition`（定义）、`theorem`（定理，含判别法/公式化的重要结论）、`property`（性质，含运算法则）。不要用其他值。
3. 全文件是**合法 JavaScript**，字符串内的反斜杠、引号要正确转义。写完后自查一遍括号是否配对。
4. 不要引入任何外部图片 URL、外部字体、外部 CDN 链接——所有内容必须是纯文本 + KaTeX 公式。
5. `id` 全局唯一，不要与其他学科文件里的 id 冲突（用学科前缀区分，如 `calc-`, `la-`, `prob-`）。
6. 文件末尾不需要任何 `module.exports`，就是一段可以直接被 `<script>` 标签执行的代码。
7. 每章条目数量不必强求一致，但核心章节（如高数的一元微分/积分、线代的特征值、概率的随机变量分布）应多覆盖一些（15+），次要章节可以少一些（8~10）。
